turbine:
  appConfig: books-service
  clusterNameExpression: "'default'"

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
  instance:
    preferIpAddress: true