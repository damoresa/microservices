server:
  port: 8090

spring:
  application:
    name: gateway-service
  cloud:
    config:
      uri: http://config:8080
      failFast: true
      retry:
        initialInterval: 2000
        maxAttempts: 30