server:
  port: 8081

spring:
  application:
    name: authors-service
  cloud:
    config:
      uri: http://config:8080
      failFast: true
      retry:
        initialInterval: 2000
        maxAttempts: 30