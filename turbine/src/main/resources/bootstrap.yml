server:
  port: 8078

spring:
  application:
    name: turbine-service
  cloud:
    config:
      uri: http://config:8080
      failFast: true
      retry:
        initialInterval: 2000
        maxAttempts: 30